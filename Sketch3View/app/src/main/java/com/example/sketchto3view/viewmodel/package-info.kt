/**
 * ViewModel layer package.
 *
 * Contains ViewModels that manage UI state and coordinate between
 * the UI layer and domain use cases.
 *
 * ViewModels in this package:
 * - HomeViewModel: manages task list state and user actions
 * - ManualCropViewModel: manages crop state and crop line adjustments
 */
package com.example.sketchto3view.viewmodel
